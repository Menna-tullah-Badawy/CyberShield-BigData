# ==============================================================================
# CyberShield-BigData: Base Application Settings (torch track)
# ==============================================================================
app:
  name: "CyberShield-BigData"
  version: "2.0.0"
  environment: "production"
  random_seed: 42
  debug: false

compute:
  device: "auto" # auto | cuda | cpu
  batch_size: 512
