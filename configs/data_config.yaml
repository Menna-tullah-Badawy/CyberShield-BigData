# ==============================================================================
# Data Pipeline & Ingestion Settings (mirrors Cell 0 constants)
# ==============================================================================
storage:
  raw_dir: "data/raw"
  store_dir: "data/feature_store/nids_features_latest"
  out_dir: "benchmark_results"
  reports_dir: "reports"
  models_dir: "models"

ingestion:
  kaggle_dataset_id: "solarmainframe/ids-intrusion-csv"
  project_dataset: "mennatullahbadawy/cybershield-bigdata-project"
  recursive_search: true

temporal_split:
  train_ratio: 0.70
  val_ratio: 0.85 # train 70% / val 15% / test 15%
  sample_step: 5
