# ==============================================================================
# Cleaning Settings (mirrors Cell 0/Cell 1 constants)
# ==============================================================================
sanitization:
  numeric_coercion: true # pd.to_numeric(errors='coerce')
  default_numeric_fill: 0.0
  nan_to_num: [0.0, 0.0, 0.0] # nan, posinf, neginf

scaling_clip:
  train_clip: 6.0
  serving_clip: 15.0
